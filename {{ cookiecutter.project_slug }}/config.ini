[path]
cache_path = cache
logs_path = logs
source_path = src
data_path = data
model_path = model
{% if cookiecutter.use_jupyter == 'y' %}notebooks_path = notebooks{% endif %}
scripts_path = scripts
test_path = tests

[logs]
level = INFO
format = %(asctime)s %(levelname)s - %(name)s - %(message)s

